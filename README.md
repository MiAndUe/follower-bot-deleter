# follower-bot-deleter
bot followers typically have high follower counts and are not followed by the user. This repo has the user login, it scans for bots among the following, checks with the user, then deletes them
